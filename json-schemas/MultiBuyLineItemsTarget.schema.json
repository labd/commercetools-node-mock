{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "additionalProperties": false,
  "title": "MultiBuyLineItemsTarget",
  "type": "object",
  "properties": {
    "discountedQuantity": {"type": "number"},
    "predicate": {"type": "string"},
    "triggerQuantity": {"type": "number"},
    "type": {"enum": ["multiBuyLineItems"]},
    "maxOccurrence": {"type": "number"},
    "selectionMode": {"$ref": "SelectionModeEnum.schema.json"}
  },
  "required": [
    "type",
    "predicate",
    "triggerQuantity",
    "discountedQuantity",
    "selectionMode"
  ],
  "$id": "https://api.commercetools.com/MultiBuyLineItemsTarget.schema.json"
}
