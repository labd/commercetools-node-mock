{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Attribute type for localized enum values. Useful for predefined language-specific values selectable in drop-down menus if only one value can be selected. Use [AttributeSetType](ctp:api:type:AttributeSetType) of AttributeLocalizedEnumValue instead if multiple values can be selected.",
  "additionalProperties": false,
  "title": "AttributeLocalizedEnumValue",
  "type": "object",
  "properties": {
    "key": {"type": "string"},
    "label": {"$ref": "LocalizedString.schema.json"}
  },
  "required": [
    "key",
    "label"
  ],
  "$id": "https://api.commercetools.com/AttributeLocalizedEnumValue.schema.json"
}
