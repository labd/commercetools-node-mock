{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Values of a SetType Custom Field are sets of values of the specified `elementType` (without duplicate elements).",
  "additionalProperties": false,
  "title": "CustomFieldSetType",
  "type": "object",
  "properties": {
    "name": {"enum": ["Set"]},
    "elementType": {"$ref": "FieldType.schema.json"}
  },
  "required": [
    "name",
    "elementType"
  ],
  "$id": "https://api.commercetools.com/CustomFieldSetType.schema.json"
}
