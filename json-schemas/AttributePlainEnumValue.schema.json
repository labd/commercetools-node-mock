{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "A plain enum value must be unique within the enum, otherwise a [DuplicateEnumValues](/errors#product-types-400-duplicate-enum-values) error will be returned.",
  "additionalProperties": false,
  "title": "AttributePlainEnumValue",
  "type": "object",
  "properties": {
    "key": {"type": "string"},
    "label": {"type": "string"}
  },
  "required": [
    "key",
    "label"
  ],
  "$id": "https://api.commercetools.com/AttributePlainEnumValue.schema.json"
}
