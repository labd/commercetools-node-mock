{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Attribute type for plain enum values. Useful for predefined language-agnostic values selectable in drop downs when only one value should be selected. Use [AttributeSetType](ctp:api:type:AttributeSetType) of AttributeEnumType instead if multiple values can be selected from the list.",
  "additionalProperties": false,
  "title": "AttributeEnumType",
  "type": "object",
  "properties": {
    "name": {"enum": ["enum"]},
    "values": {
      "type": "array",
      "items": {"$ref": "AttributePlainEnumValue.schema.json"}
    }
  },
  "required": [
    "name",
    "values"
  ],
  "$id": "https://api.commercetools.com/AttributeEnumType.schema.json"
}
