{
  "oneOf": [{"$ref": "https://api.commercetools.com/StoreKeyReference.schema.json"}],
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "A KeyReference represents a loose reference to another resource in the same Project identified by the resource's `key` field. If available, the `key` is immutable and mandatory. KeyReferences do not support [Reference Expansion](/general-concepts#reference-expansion).",
  "additionalProperties": false,
  "title": "KeyReference",
  "type": "object",
  "properties": {
    "typeId": {"enum": ["store"]},
    "key": {"type": "string"}
  },
  "required": [
    "typeId",
    "key"
  ],
  "$id": "https://api.commercetools.com/KeyReference.schema.json",
  "discriminator": {"propertyName": "typeId"}
}
