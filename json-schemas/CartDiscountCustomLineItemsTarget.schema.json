{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Discount is applied to [CustomLineItems](ctp:api:type:CustomLineItem) matching the `predicate`.",
  "additionalProperties": false,
  "title": "CartDiscountCustomLineItemsTarget",
  "type": "object",
  "properties": {
    "type": {"enum": ["customLineItems"]},
    "predicate": {"type": "string"}
  },
  "required": [
    "type",
    "predicate"
  ],
  "$id": "https://api.commercetools.com/CartDiscountCustomLineItemsTarget.schema.json"
}
