{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Updates the `attributeConstraint` of an [AttributeDefinition](ctp:api:type:AttributeDefinition). For now only following changes are supported: `SameForAll` to `None` and `Unique` to `None`.",
  "additionalProperties": false,
  "title": "ProductTypeChangeAttributeConstraintAction",
  "type": "object",
  "properties": {
    "action": {"enum": ["changeAttributeConstraint"]},
    "newValue": {"$ref": "AttributeConstraintEnumDraftEnum.schema.json"},
    "attributeName": {"type": "string"}
  },
  "required": [
    "action",
    "attributeName",
    "newValue"
  ],
  "$id": "https://api.commercetools.com/ProductTypeChangeAttributeConstraintAction.schema.json"
}
