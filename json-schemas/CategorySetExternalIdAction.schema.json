{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "This update action sets a new ID that can be used as an additional identifier for external systems like Customer Relationship Management (CRM) or Enterprise Resource Planning (ERP).",
  "additionalProperties": false,
  "title": "CategorySetExternalIdAction",
  "type": "object",
  "properties": {
    "action": {"enum": ["setExternalId"]},
    "externalId": {"type": "string"}
  },
  "required": ["action"],
  "$id": "https://api.commercetools.com/CategorySetExternalIdAction.schema.json"
}
