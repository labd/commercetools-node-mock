{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Used when the ShippingRate maps to an abstract Cart categorization expressed by integers (such as shipping scores or weight ranges).\nEither `price` or `priceFunction` is required.",
  "additionalProperties": false,
  "title": "CartScoreTier",
  "type": "object",
  "properties": {
    "score": {"type": "number"},
    "type": {"enum": ["CartScore"]},
    "priceFunction": {"$ref": "PriceFunction.schema.json"},
    "isMatching": {"type": "boolean"},
    "price": {"$ref": "Money.schema.json"}
  },
  "required": [
    "type",
    "score"
  ],
  "$id": "https://api.commercetools.com/CartScoreTier.schema.json"
}
