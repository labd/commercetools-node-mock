{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "title": "OrderUpdateSyncInfoAction",
  "type": "object",
  "properties": {
    "channel": {"$ref": "ChannelResourceIdentifier.schema.json"},
    "action": {"enum": ["updateSyncInfo"]},
    "externalId": {"type": "string"},
    "syncedAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "action",
    "channel"
  ],
  "$id": "https://example.com/OrderUpdateSyncInfoAction.schema.json"
}
