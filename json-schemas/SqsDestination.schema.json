{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "title": "SqsDestination",
  "type": "object",
  "properties": {
    "queueUrl": {"type": "string"},
    "type": {"enum": ["SQS"]},
    "region": {"type": "string"},
    "accessKey": {"type": "string"},
    "accessSecret": {"type": "string"}
  },
  "required": [
    "type",
    "accessKey",
    "accessSecret",
    "queueUrl",
    "region"
  ],
  "$id": "https://example.com/SqsDestination.schema.json"
}
