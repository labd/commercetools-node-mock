{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Discount is applied to [LineItems](ctp:api:type:LineItem) matching the `predicate`.",
  "additionalProperties": false,
  "title": "CartDiscountLineItemsTarget",
  "type": "object",
  "properties": {
    "type": {"enum": ["lineItems"]},
    "predicate": {"type": "string"}
  },
  "required": [
    "type",
    "predicate"
  ],
  "$id": "https://api.commercetools.com/CartDiscountLineItemsTarget.schema.json"
}
