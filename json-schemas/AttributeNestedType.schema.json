{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Attribute type for nesting Attributes based on some existing ProductType. It does not support `isSearchable` and is not supported in queries. The only supported AttributeConstraint is `None`.",
  "additionalProperties": false,
  "title": "AttributeNestedType",
  "type": "object",
  "properties": {
    "name": {"enum": ["nested"]},
    "typeReference": {"$ref": "ProductTypeReference.schema.json"}
  },
  "required": [
    "name",
    "typeReference"
  ],
  "$id": "https://api.commercetools.com/AttributeNestedType.schema.json"
}
