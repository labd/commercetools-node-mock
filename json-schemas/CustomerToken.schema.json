{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "title": "CustomerToken",
  "type": "object",
  "properties": {
    "customerId": {"type": "string"},
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "id": {"type": "string"},
    "lastModifiedAt": {
      "type": "string",
      "format": "date-time"
    },
    "value": {"type": "string"},
    "expiresAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "id",
    "createdAt",
    "customerId",
    "expiresAt",
    "value"
  ],
  "$id": "https://example.com/CustomerToken.schema.json"
}
