{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Specifies a Price tier that applies when the minimum quantity for the [LineItem](ctp:api:type:LineItem) of a ProductVariant with the related Price is reached in a Cart.",
  "additionalProperties": false,
  "title": "PriceTierDraft",
  "type": "object",
  "properties": {
    "minimumQuantity": {"type": "number"},
    "value": {"$ref": "Money.schema.json"}
  },
  "required": [
    "minimumQuantity",
    "value"
  ],
  "$id": "https://api.commercetools.com/PriceTierDraft.schema.json"
}
