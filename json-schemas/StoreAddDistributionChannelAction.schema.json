{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "This action has no effect if a given distribution channel is already present in a Store.",
  "additionalProperties": false,
  "title": "StoreAddDistributionChannelAction",
  "type": "object",
  "properties": {
    "distributionChannel": {"$ref": "ChannelResourceIdentifier.schema.json"},
    "action": {"enum": ["addDistributionChannel"]}
  },
  "required": [
    "action",
    "distributionChannel"
  ],
  "$id": "https://api.commercetools.com/StoreAddDistributionChannelAction.schema.json"
}
