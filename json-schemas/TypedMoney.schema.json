{
  "oneOf": [
    {"$ref": "https://api.commercetools.com/CentPrecisionMoney.schema.json"},
    {"$ref": "https://api.commercetools.com/HighPrecisionMoney.schema.json"}
  ],
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Base polymorphic read-only Money type which is stored in cent precision or high precision. The actual type is determined by the `type` field.",
  "additionalProperties": false,
  "title": "TypedMoney",
  "type": "object",
  "properties": {
    "centAmount": {"type": "number"},
    "fractionDigits": {"type": "number"},
    "type": {"enum": [
      "centPrecision",
      "highPrecision"
    ]},
    "currencyCode": {"type": "string"}
  },
  "required": [
    "centAmount",
    "currencyCode",
    "type",
    "fractionDigits"
  ],
  "$id": "https://api.commercetools.com/TypedMoney.schema.json",
  "discriminator": {"propertyName": "type"}
}
