{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "Discounts the Product Price by allowing the client to explicitly [set a discounted value](/../api/projects/products#set-discounted-embedded-price).\nUsed when setting discounts using an external service.",
  "additionalProperties": false,
  "title": "ProductDiscountValueExternal",
  "type": "object",
  "properties": {"type": {"enum": ["external"]}},
  "required": ["type"],
  "$id": "https://api.commercetools.com/ProductDiscountValueExternal.schema.json"
}
