{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "This action has no effect if a given supply channel is already present in a Store.",
  "additionalProperties": false,
  "title": "StoreAddSupplyChannelAction",
  "type": "object",
  "properties": {
    "action": {"enum": ["addSupplyChannel"]},
    "supplyChannel": {"$ref": "ChannelResourceIdentifier.schema.json"}
  },
  "required": [
    "action",
    "supplyChannel"
  ],
  "$id": "https://api.commercetools.com/StoreAddSupplyChannelAction.schema.json"
}
