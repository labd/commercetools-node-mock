{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "If a language is used by a [Store](ctp:api:type:Store), it cannot be deleted. Attempts to delete such language will lead to [LanguageUsedInStores](/../api/errors#projects-400-language-used-in-stores) errors.",
  "additionalProperties": false,
  "title": "ProjectChangeLanguagesAction",
  "type": "object",
  "properties": {
    "languages": {
      "type": "array",
      "items": {"type": "string"}
    },
    "action": {"enum": ["changeLanguages"]}
  },
  "required": [
    "action",
    "languages"
  ],
  "$id": "https://api.commercetools.com/ProjectChangeLanguagesAction.schema.json"
}
