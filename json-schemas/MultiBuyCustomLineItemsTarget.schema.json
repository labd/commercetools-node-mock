{
  "$schema": "https://json-schema.org/draft/2020-12/schema#",
  "description": "This Discount target is similar to `MultiBuyLineItems`, but is applied on Custom Line Items instead of Line Items.",
  "additionalProperties": false,
  "title": "MultiBuyCustomLineItemsTarget",
  "type": "object",
  "properties": {
    "discountedQuantity": {"type": "number"},
    "predicate": {"type": "string"},
    "triggerQuantity": {"type": "number"},
    "type": {"enum": ["multiBuyCustomLineItems"]},
    "maxOccurrence": {"type": "number"},
    "selectionMode": {"$ref": "SelectionModeEnum.schema.json"}
  },
  "required": [
    "type",
    "predicate",
    "triggerQuantity",
    "discountedQuantity",
    "selectionMode"
  ],
  "$id": "https://api.commercetools.com/MultiBuyCustomLineItemsTarget.schema.json"
}
